apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wjb

resources:
  - persistent-volume.yaml
  - persistent-volume-claim.yaml
  - statefulset.yaml
  - service.yaml
  - secret.yaml
