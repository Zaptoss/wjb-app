apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-admin
  template:
    metadata:
      labels:
        app: api-admin
    spec:
      imagePullSecrets:
        - name: gar-pull
      containers:
        - name: api-admin
          image: europe-central2-docker.pkg.dev/int20h-2026/wjb/api-admin:v0.1.0 # {"$imagepolicy": "wjb:admin-backend"}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_URLS
              value: "http://0.0.0.0:8080"
            - name: ConnectionStrings__LocalhostConnection
              valueFrom:
                secretKeyRef:
                  name: wjb-api-secret
                  key: CONNECTION_STRING
            - name: Jwt__Secret
              valueFrom:
                secretKeyRef:
                  name: wjb-api-secret
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
